apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql
  namespace: laravel-multi-pod
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
